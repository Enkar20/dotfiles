# dotfiles

Personal dotfiles for Arch Linux & WSL.

## Included
- tmux configuration (layout-agnostic, WSL-safe)
- bash configuration (minimal)

## Philosophy
- No heavy frameworks
- No magic
- Explicit configs
- Works on WSL and bare metal

## Usage
```bash
ln -s ~/dotfiles/tmux.conf ~/.tmux.conf
